{
	"name": "hstack",
	"version": "0.2.0",
	"private": true,
	"type": "module",
	"description": "Cursor plugin: native-tools enforcement via shell interception",
	"scripts": {
		"test": "bun test",
		"check": "bunx tsc --noEmit"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.8.0"
	}
}
