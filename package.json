{
	"name": "hstack",
	"version": "0.4.3",
	"private": true,
	"type": "module",
	"description": "Cursor plugin stack (shell interceptor + more)",
	"scripts": {
		"test": "bun test ./test",
		"check": "bunx tsc --noEmit"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.8.0"
	}
}
